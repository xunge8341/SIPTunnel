# Code generated by cmd/configdocgen; DO NOT EDIT.
storage:
  temp_dir: ./data/temp
  final_dir: ./data/final
  audit_dir: ./data/audit
  log_dir: ./data/logs

network:
  sip:
    enabled: true
    listen_ip: 127.0.0.1
    listen_port: 5060
    transport: TCP
    advertise_ip: ""
    domain: ""
    max_message_bytes: 4096
    read_timeout_ms: 5000
    write_timeout_ms: 5000
    idle_timeout_ms: 60000

  rtp:
    enabled: true
    listen_ip: 127.0.0.1
    advertise_ip: ""
    port_start: 20000
    port_end: 20999
    transport: UDP
    max_packet_bytes: 1400
    tcp_read_timeout_ms: 5000
    tcp_write_timeout_ms: 5000
    tcp_keepalive_enabled: true
    max_tcp_sessions: 128
    max_inflight_transfers: 8
    receive_buffer_bytes: 4194304
    transfer_timeout_ms: 30000
    retransmit_max_rounds: 3

media:
  port_range:
    start: 20000
    end: 20999

node:
  role: receiver

transport_tuning:
  # 性能优先、弱网兜底的传输策略档位。
  # 可选/校验值：可选值：secure_boundary / boundary / strict_boundary
  # 默认值：secure_boundary
  mode: secure_boundary
  # UDP 控制面单报文预算。
  # 可选/校验值：推荐 1300；范围 [1024,1400]；低于 1249 会重现现场 oversize
  # 默认值：1300
  udp_control_max_bytes: 1300
  # 目录/分页查询控制面预算。
  # 可选/校验值：推荐 1300；范围 [1024,1400]
  # 默认值：1300
  udp_catalog_max_bytes: 1300
  # INLINE 响应可用 UDP 预算。
  # 可选/校验值：推荐 1200；范围 [1024,1400]
  # 默认值：1200
  inline_response_udp_budget_bytes: 1200
  # INLINE 安全预留字节。
  # 可选/校验值：推荐 220；范围 [128,512]
  # 默认值：220
  inline_response_safety_reserve_bytes: 220
  # INLINE XML/封套开销估算。
  # 可选/校验值：推荐 320；范围 [128,768]
  # 默认值：320
  inline_response_envelope_overhead_bytes: 320
  # INLINE 预算头寸比例。
  # 可选/校验值：推荐 0.15；范围 [0.05,0.30]
  # 默认值：0.15
  inline_response_headroom_ratio: 0.15
  # 兼容旧配置的 headroom 百分比。
  # 可选/校验值：兼容字段；推荐与 ratio 保持一致
  # 默认值：15
  inline_response_headroom_percent: 15
  # 单设备普通请求并发。
  # 可选/校验值：推荐 4；范围 [2,16]
  # 默认值：6
  udp_request_parallelism_per_device: 6
  # 单对端 callback 并发。
  # 可选/校验值：推荐 4；范围 [2,16]
  # 默认值：6
  udp_callback_parallelism_per_peer: 6
  # 单设备 bulk_open 并发。
  # 可选/校验值：推荐 4；范围 [2,16]
  # 默认值：4
  udp_bulk_parallelism_per_device: 4
  # 单设备 segment-child 总并发。
  # 可选/校验值：推荐 8；范围 [2,16]；回环/同源同看场景建议 4~8
  # 默认值：8
  udp_segment_parallelism_per_device: 8
  # small lane 最大等待时间。
  # 可选/校验值：推荐 1500；范围 [500,5000]
  # 默认值：1500
  udp_small_request_max_wait_ms: 1500
  # 热点播放窗口大小，用内存换更少控制面往返。
  # 可选/校验值：推荐 8MiB；范围 [4MiB,16MiB]
  # 默认值：8388608
  adaptive_playback_hot_window_bytes: 8388608
  # 热点段缓存总容量。
  # 可选/校验值：推荐 512MiB；范围 [128MiB,1GiB]
  # 默认值：536870912
  adaptive_playback_segment_cache_bytes: 536870912
  # 热点段缓存 TTL。
  # 可选/校验值：推荐 45000；范围 [10000,300000]
  # 默认值：45000
  adaptive_playback_segment_cache_ttl_ms: 45000
  # 热点播放预取段数。
  # 可选/校验值：推荐 1；范围 [0,2]；稳定态默认不预取，仅热点命中后再开
  # 默认值：1
  adaptive_playback_prefetch_segments: 1
  # 连续流失败多少次后切到 segmented_primary。
  # 可选/校验值：推荐 2；范围 [1,4]
  # 默认值：2
  adaptive_primary_segment_after_failures: 2
  # 本地回环源场景下的稳定态 segment 并发。
  # 可选/校验值：推荐 2；范围 [1,4]；过低会让播放体感发钝，过高会重新放大回环带宽
  # 默认值：2
  adaptive_loopback_playback_segment_concurrency: 2
  # recent probe abort 后，将 bytes=N- 改写成首个有界窗口的大小。
  # 可选/校验值：推荐 8388608(8MiB)；范围 [1048576,33554432]；用于避免先拉全量再切段
  # 默认值：8388608
  adaptive_open_ended_range_initial_window_bytes: 8388608
  # 边界 RTP payload 大小。
  # 可选/校验值：推荐 1200；范围 [960,1200]
  # 默认值：1200
  boundary_rtp_payload_bytes: 1200
  # 非音视频大文件进入 segmented-primary 的阈值。
  # 可选/校验值：推荐 8388608(8MiB)；范围 [8388608,536870912]；更早进入 segmented-primary，避免慢客户端下载长期卡在单流
  # 默认值：8388608
  generic_segmented_primary_threshold_bytes: 8388608
  # 非音视频大文件 segmented-primary 的预取段数。
  # 可选/校验值：推荐 0；范围 [0,2]；默认关闭预取，避免下载侧额外放大回源并发
  # 默认值：0
  generic_prefetch_segments: 0
  # 非音视频大文件默认分段窗口。
  # 可选/校验值：推荐 2097152(2MiB)；范围 [1048576,67108864]；优先缩短弱网失败时的回退长度，避免单段过大
  # 默认值：2097152
  generic_download_window_bytes: 2097152
  generic_download_open_ended_window_bytes: 16777216
  # 单个非音视频下载默认 segment 并发；大文件开区间 Range 在运行时还会自动降到单并发保守模式。
  # 可选/校验值：推荐 1；范围 [1,8]；开区间 Range/熔断打开时会自动收敛到 1
  # 默认值：1
  generic_download_segment_concurrency: 1
  generic_download_same_transfer_split_enabled: false
  generic_download_source_constrained_auto_singleflight_enabled: false
  # 单个下载 segment 重试次数。
  # 可选/校验值：推荐 2；范围 [0,6]
  # 默认值：2
  generic_download_segment_retries: 2
  # 单个非音视频下载事务的总恢复次数。
  # 可选/校验值：推荐 6；范围 [1,12]
  # 默认值：6
  generic_download_resume_max_attempts: 6
  # 同一下载窗口内的恢复重试次数。
  # 可选/校验值：推荐 3；范围 [1,6]
  # 默认值：3
  generic_download_resume_per_range_retries: 3
  # 下载类 rtp_sequence_gap 触发后的附加等待。
  # 可选/校验值：推荐 500；范围 [0,5000]；默认显著小于设备级 10s 罚等
  # 默认值：500
  generic_download_penalty_wait_ms: 500
  # 所有非音视频外层下载事务共享的总发送预算。
  # 可选/校验值：推荐 33554432；范围 [4194304,134217728]；按外层下载事务而不是按 segment child 公平分享带宽
  # 默认值：33554432
  generic_download_total_bitrate_bps: 33554432
  # 单个外层下载事务在公平整形下的最低发送速率；只有总预算足以覆盖所有活跃下载时才会启用。
  # 可选/校验值：推荐 2097152；范围 [1048576,33554432]；不会再按 segment child 逐个叠加打穿总带宽
  # 默认值：2097152
  generic_download_min_per_transfer_bitrate_bps: 2097152
  # 下载类连续失败达到多少次后进入熔断降级。
  # 可选/校验值：推荐 3；范围 [1,10]
  # 默认值：3
  generic_download_circuit_failure_threshold: 3
  # 下载类熔断打开时长。
  # 可选/校验值：推荐 30000；范围 [1000,300000]
  # 默认值：30000
  generic_download_circuit_open_ms: 30000
  # 下载专用 RTP 整形速率上限。
  # 可选/校验值：推荐 8388608；范围 [2097152,67108864]；现场日志显示实际稳态远低于 16Mbps，默认改得更保守
  # 默认值：8388608
  generic_download_rtp_bitrate_bps: 8388608
  # 下载专用 RTP 包间最小间隔。
  # 可选/校验值：推荐 650；范围 [100,10000]；适当拉大 spacing，减少大文件下载的瞬时 burst
  # 默认值：650
  generic_download_rtp_min_spacing_us: 650
  # 下载专用 RTP 套接字缓冲。
  # 可选/校验值：推荐 33554432(32MiB)；范围 [1048576,67108864]；用内存换更强乱序吸收
  # 默认值：33554432
  generic_download_rtp_socket_buffer_bytes: 33554432
  # 下载专用 RTP 乱序窗口。
  # 可选/校验值：推荐 512；范围 [16,1024]
  # 默认值：512
  generic_download_rtp_reorder_window_packets: 512
  # 下载专用 RTP 丢包容忍。
  # 可选/校验值：推荐 192；范围 [8,512]
  # 默认值：192
  generic_download_rtp_loss_tolerance_packets: 192
  # 下载专用 RTP gap 等待超时。
  # 可选/校验值：推荐 900；范围 [100,5000]
  # 默认值：900
  generic_download_rtp_gap_timeout_ms: 900
  generic_download_rtp_fec_enabled: true
  generic_download_rtp_fec_group_packets: 8
  # 边界 RTP 发送整形速率。
  # 可选/校验值：推荐 16777216；范围 [2097152,67108864]；从 legacy 3Mbps profile 切回 boundary-rtp 后，避免默认过猛
  # 默认值：16777216
  boundary_rtp_bitrate_bps: 16777216
  # 边界 RTP 包间最小间隔。
  # 可选/校验值：推荐 250；范围 [100,5000]；越小越激进，需配合链路质量
  # 默认值：250
  boundary_rtp_min_spacing_us: 250
  # 边界 RTP 套接字读写缓存。
  # 可选/校验值：推荐 16777216(16MiB)；范围 [1048576,67108864]；用内存换 burst 吞吐与弱网抖动容忍
  # 默认值：16777216
  boundary_rtp_socket_buffer_bytes: 16777216
  # 边界 RTP 乱序窗口。
  # 可选/校验值：推荐 128；范围 [16,512]
  # 默认值：128
  boundary_rtp_reorder_window_packets: 128
  # 边界 RTP 丢包容忍。
  # 可选/校验值：推荐 48；范围 [0,256]
  # 默认值：48
  boundary_rtp_loss_tolerance_packets: 48
  # 边界 RTP gap 等待超时。
  # 可选/校验值：推荐 300；范围 [100,30000]
  # 默认值：300
  boundary_rtp_gap_timeout_ms: 300
  boundary_rtp_fec_enabled: true
  boundary_rtp_fec_group_packets: 8
  # 播放场景 RTP 乱序窗口。
  # 可选/校验值：推荐 192；范围 [32,512]
  # 默认值：192
  boundary_playback_rtp_reorder_window_packets: 192
  # 播放场景 RTP 丢包容忍。
  # 可选/校验值：推荐 64；范围 [8,256]
  # 默认值：64
  boundary_playback_rtp_loss_tolerance_packets: 64
  # 播放场景 RTP gap 等待超时。
  # 可选/校验值：推荐 450；范围 [100,30000]
  # 默认值：450
  boundary_playback_rtp_gap_timeout_ms: 450
  boundary_playback_rtp_fec_enabled: true
  boundary_playback_rtp_fec_group_packets: 8
  # 边界 fallback 分段窗口大小。
  # 可选/校验值：推荐 4MiB；范围 [1MiB,8MiB]
  # 默认值：4194304
  boundary_fixed_window_bytes: 4194304
  # 边界大文件进入 fallback 参考阈值。
  # 可选/校验值：推荐 256MiB；范围 [64MiB,1GiB]
  # 默认值：268435456
  boundary_fixed_window_threshold_bytes: 268435456
  # 边界 fallback 分段并发。
  # 可选/校验值：推荐 4；范围 [1,16]
  # 默认值：4
  boundary_segment_concurrency: 4
  # 边界 fallback 分段重试次数。
  # 可选/校验值：推荐 1；范围 [0,2]
  # 默认值：1
  boundary_segment_retries: 1
  # 边界流主路径最大 resume 次数。
  # 可选/校验值：推荐 3；范围 [1,4]
  # 默认值：3
  boundary_resume_max_attempts: 3
  # 单窗口 range 重试次数。
  # 可选/校验值：推荐 1；范围 [0,2]
  # 默认值：1
  boundary_resume_per_range_retries: 1
  # 边界主请求 response_start 等待。
  # 可选/校验值：推荐 12000；范围 [8000,30000]
  # 默认值：12000
  boundary_response_start_wait_ms: 12000
  # 边界 range/segment response_start 等待。
  # 可选/校验值：推荐 45000；范围 [15000,60000]
  # 默认值：45000
  boundary_range_response_start_wait_ms: 45000
  # 边界 HTTP fallback 窗口大小。
  # 可选/校验值：推荐 4MiB；范围 [1MiB,8MiB]
  # 默认值：4194304
  boundary_http_window_bytes: 4194304
  # 边界 HTTP fallback 参考阈值。
  # 可选/校验值：推荐 256MiB；范围 [64MiB,1GiB]
  # 默认值：268435456
  boundary_http_window_threshold_bytes: 268435456
  # 边界 HTTP fallback 并发。
  # 可选/校验值：推荐 4；范围 [1,16]
  # 默认值：4
  boundary_http_segment_concurrency: 4
  # 边界 HTTP fallback 重试次数。
  # 可选/校验值：推荐 1；范围 [0,2]
  # 默认值：1
  boundary_http_segment_retries: 1
  # 普通网络 fallback 窗口大小。
  # 可选/校验值：推荐 16MiB；范围 [4MiB,32MiB]
  # 默认值：16777216
  standard_window_bytes: 16777216
  # 普通网络 fallback 参考阈值。
  # 可选/校验值：推荐 256MiB；范围 [64MiB,1GiB]
  # 默认值：268435456
  standard_window_threshold_bytes: 268435456
  # 普通网络 fallback 分段并发。
  # 可选/校验值：推荐 4；范围 [1,16]
  # 默认值：4
  standard_segment_concurrency: 4
  # 普通网络 fallback 重试次数。
  # 可选/校验值：推荐 1；范围 [0,2]
  # 默认值：1
  standard_segment_retries: 1
